import { StyleSheet } from 'react-native';
export const GlobalStyle = StyleSheet.create({
  tapBtn: {
    width: 108,
    paddingVertical: 10,
    paddingHorizontal: 20,
    borderRadius: 25,
    alignItems: 'center',
  },
});
